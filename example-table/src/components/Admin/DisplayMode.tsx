{/*
import React from "react";
import { Outlet, useNavigate } from "react-router-dom";

const DisplayModeLayout = () => {
    const navigate = useNavigate();

    const goToDashboard = () => {
        navigate("/admin");
    };

    return (
        <div style={{ height: "100vh", width: "100vw", display: "flex", flexDirection: "column", backgroundColor: "#f0f0f0" }}>
            <div style={{ flex: 1, display: "flex", justifyContent: "center", alignItems: "center" }}>
                <Outlet />
            </div>
            <div style={{ textAlign: "center", padding: "10px" }}>
                <button onClick={goToDashboard} style={{ padding: "10px 20px", fontSize: "16px", cursor: "pointer" }}>
                    Back to Dashboard
                </button>
            </div>
        </div>
    );
};

export default DisplayModeLayout;
*/}
